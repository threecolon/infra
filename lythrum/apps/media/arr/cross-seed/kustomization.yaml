apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - hr.yaml
  - api-key.secret.yaml
configMapGenerator:
  - name: cross-seed-config
    namespace: media
    files:
      - config/config.js
configurations:
  - kustomizeconfig.yaml