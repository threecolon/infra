apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cron.yaml
configMapGenerator:
  - name: configarr-config
    files:
      - config.yaml
configurations:
  - kustomizeconfig.yaml