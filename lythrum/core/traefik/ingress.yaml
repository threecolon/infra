apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traefik
  namespace: traefik
spec:
  defaultBackend:
    service:
      name: traefik
      port:
        name: web
  ingressClassName: tailscale
  tls:
    - hosts:
        - ${SUBDOMAIN_TRAEFIK}